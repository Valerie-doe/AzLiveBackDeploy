# AZLive backend — dépendances de production
# (versions alignées sur l'environnement local pour un comportement identique)

Django==6.0.6
djangorestframework==3.17.1
django-cors-headers==4.9.0
psycopg2-binary==2.9.12
Pillow==12.0.0
reportlab==4.4.4

# Serveur WSGI de production + fichiers statiques + parsing DATABASE_URL
gunicorn==23.0.0
whitenoise==6.8.2
dj-database-url==2.3.0
